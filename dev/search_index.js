var documenterSearchIndex = {"docs":
[{"category":"section","location":"walks/#Walking-a-parameter-set","page":"Walking a parameter set","text":"Nearly everything done to a parameter set has the same shape: recurse into the NamedTuples, do something at each leaf, put the result back in the same shape. Moving parameters to another device, mapping them to the host, making them static, the elementwise arithmetic an optimizer cache needs, writing them to a file — all of it.\n\nWritten once over the leaf protocol, the recursion never needs to know which structured types exist, so each of those operations becomes a call rather than its own copy of the traversal with a method per wrapper type.","title":"Walking a parameter set"},{"category":"section","location":"walks/#Whole-leaves-or-their-storage","page":"Walking a parameter set","text":"The distinction between mapparameters and mapstorage is which level the function sees, and both are needed.\n\nMoving a parameter to another device wants the whole leaf: it has to rebuild the wrapper around the moved array. Halving one wants only the storage: for a symmetric matrix that means halving the n(n+1)2 numbers it keeps, and broadcasting over the dense interface instead would do twice the work — while for a skew-symmetric or triangular matrix there is no setindex! to broadcast through at all.\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]), L2 = (W = [4.0 5.0],)))\nmapparameters(x -> x ./ 2, ps).L1.W\n\n# output\n\n1×2 Matrix{Float64}:\n 0.5  1.0","title":"Whole leaves or their storage"},{"category":"section","location":"walks/#Combining-two-sets","page":"Walking a parameter set","text":"Given more than one argument the trees are walked in lockstep and the function receives one leaf from each:\n\na = NetworkParameters((L = (x = [1.0, 2.0],),))\nb = NetworkParameters((L = (x = [10.0, 20.0],),))\nmapparameters(+, a, b).L.x\n\n# output\n\n2-element Vector{Float64}:\n 11.0\n 22.0","title":"Combining two sets"},{"category":"section","location":"walks/#Gaps","page":"Walking a parameter set","text":"A branch or leaf that is nothing skips that position in the in-place and foreach walks. This is the shape a gradient tree has when a layer was frozen or simply not differentiated, and skipping means such a tree can be walked against the parameters it belongs to without filling the holes in first:\n\ndest = NetworkParameters((p = [1.0], q = [2.0]))\nmapparameters!((d, s) -> (d .+= s), dest, NetworkParameters((p = [10.0], q = nothing)))\n(dest.p, dest.q)\n\n# output\n\n([11.0], [2.0])","title":"Gaps"},{"category":"section","location":"walks/#Reductions","page":"Walking a parameter set","text":"foldparameters visits leaves in the order flatten writes them, so a fold and a flattening agree about the order:\n\nfoldparameters((n, x) -> n + length(x), 0, ps)\n\n# output\n\n5","title":"Reductions"},{"category":"function","location":"walks/#NeuralNetworkParameters.mapparameters","page":"Walking a parameter set","text":"mapparameters(f, ps)\nmapparameters(f, ps, rest...)\n\nApply f to every leaf of ps, returning a parameter set of the same shape.\n\nWith further arguments the trees are walked in lockstep and f receives one leaf from each, which is how two parameter sets are combined entrywise. Their keys have to agree.\n\nf sees whole leaves — a SymmetricMatrix arrives as a SymmetricMatrix. Use mapstorage to see only the differentiable storage instead.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nmapparameters(x -> 2x, ps).L1.W\n\n# output\n\n1×2 Matrix{Float64}:\n 2.0  4.0\n\nCombining two sets:\n\nusing NeuralNetworkParameters\n\na = NetworkParameters((L1 = (b = [1.0, 2.0],),))\nc = NetworkParameters((L1 = (b = [10.0, 20.0],),))\nmapparameters(+, a, c).L1.b\n\n# output\n\n2-element Vector{Float64}:\n 11.0\n 22.0\n\n\n\n\n\n","title":"NeuralNetworkParameters.mapparameters"},{"category":"function","location":"walks/#NeuralNetworkParameters.mapstorage","page":"Walking a parameter set","text":"mapstorage(f, ps, rest...)\n\nLike mapparameters, but f is applied to the freeparameters of each leaf and the leaf is rebuilded around the result.\n\nThis is the level at which entrywise arithmetic on a structured parameter is meaningful. Halving a SymmetricMatrix means halving the n(n+1)2 numbers it stores; broadcasting over its dense n times n interface would do twice the work, and for a skew-symmetric or triangular matrix there is no setindex! to broadcast through at all.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0; 3.0 4.0],),))\nmapstorage(x -> x ./ 2, ps).L1.W\n\n# output\n\n2×2 Matrix{Float64}:\n 0.5  1.0\n 1.5  2.0\n\n\n\n\n\n","title":"NeuralNetworkParameters.mapstorage"},{"category":"function","location":"walks/#NeuralNetworkParameters.mapparameters!","page":"Walking a parameter set","text":"mapparameters!(f, dest, srcs...)\n\nWalk dest and srcs in lockstep, calling f(dest_leaf, src_leaves...) for its effect on dest_leaf, and return dest.\n\nAs with foreachparameters, a nothing in srcs skips that position.\n\n\n\n\n\n","title":"NeuralNetworkParameters.mapparameters!"},{"category":"function","location":"walks/#NeuralNetworkParameters.mapstorage!","page":"Walking a parameter set","text":"mapstorage!(f, dest, srcs...)\n\nLike mapparameters!, but f is handed the freeparameters of each leaf.\n\nNo rebuild is needed: the storage of a leaf is the leaf's own memory, so writing into it is writing into the leaf.\n\n\n\n\n\n","title":"NeuralNetworkParameters.mapstorage!"},{"category":"function","location":"walks/#NeuralNetworkParameters.foreachparameters","page":"Walking a parameter set","text":"foreachparameters(f, ps, rest...)\n\nWalk the leaves of ps for the side effect of f, in lockstep with rest. Returns nothing.\n\nA branch or leaf of rest that is nothing skips that position entirely — f is not called there. This is what lets a gradient tree that is missing the entries of a frozen or non-trainable layer be walked against the parameters it belongs to, without having to fill the holes in first.\n\nSee mapparameters! for the in-place variant that returns its destination.\n\n\n\n\n\n","title":"NeuralNetworkParameters.foreachparameters"},{"category":"function","location":"walks/#NeuralNetworkParameters.foldparameters","page":"Walking a parameter set","text":"foldparameters(op, init, ps)\n\nLeft-fold op over the leaves of ps, starting from init.\n\nThe leaves are visited in the order flatten writes them, so a fold and a flattening agree.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]), L2 = (W = [4.0;;],)))\nfoldparameters((n, x) -> n + length(x), 0, ps)\n\n# output\n\n4\n\n\n\n\n\n","title":"NeuralNetworkParameters.foldparameters"},{"category":"section","location":"protocol/#The-leaf-protocol","page":"The leaf protocol","text":"Not every parameter is a plain array. GeometricOptimizers has a family of matrices that keep n(npm1)2 numbers behind an n times n interface, manifold elements, and horizontal lifts that keep their freedom in two blocks. Those stored numbers are the parameters: they are what belongs in the flat vector, and what an optimizer should move.\n\nTwo methods say so.\n\nWith them defined, the type flattens, walks and saves — nothing in this package holds a list of which structured types exist. That matters because the types live upstream of the package that trains with them: GeometricOptimizers owns them, GeometricMachineLearning uses them, and a serialiser driven by a list rather than a protocol would have to be written by somebody who owns neither the types nor the generic it dispatches on.","title":"The leaf protocol"},{"category":"section","location":"protocol/#An-example","page":"The leaf protocol","text":"using NeuralNetworkParameters\nimport NeuralNetworkParameters as NNP\n\nstruct Sym{T} <: AbstractMatrix{T}\n    S::Vector{T}      # n(n+1)/2 numbers\n    n::Int\nend\n\nBase.size(A::Sym) = (A.n, A.n)\nBase.getindex(A::Sym, i::Int, j::Int) = A.S[(max(i,j)*(max(i,j)-1))÷2 + min(i,j)]\n\nNNP.freeparameters(A::Sym) = A.S\nNNP.rebuild(A::Sym, data) = Sym(data, A.n)\n\nps = NetworkParameters((L1 = (S = Sym([1.0, 2.0, 3.0], 2),),))\nv, layout = flatten(ps)\nv\n\n# output\n\n3-element Vector{Float64}:\n 1.0\n 2.0\n 3.0\n\nThree numbers, not four: the flat vector holds the free parameters, and the round trip returns the same type, with its n intact.\n\nback = unflatten(layout, [10.0, 20.0, 30.0])\n(typeof(back.L1.S) === typeof(ps.L1.S), back.L1.S.n)\n\n# output\n\n(true, 2)","title":"An example"},{"category":"section","location":"protocol/#Why-rebuild-takes-a-prototype","page":"The leaf protocol","text":"rebuild is handed the leaf the storage came from, not just its type. That carries the non-differentiable information across — the n above — and it is what lets data have a different element type from the prototype, as it does under forward-mode differentiation.\n\nIt also keeps the concrete type honest. Reconstructing from a type name instead has a failure mode that has actually been hit upstream: a hardcoded reconstructor turned every manifold element into a StiefelManifold, quietly converting a GrassmannManifold on each round trip.","title":"Why rebuild takes a prototype"},{"category":"section","location":"protocol/#Adopting-it-for-a-whole-family-at-once","page":"The leaf protocol","text":"GeometricOptimizers already exposes this relation, as Base.parent: parent of its VectorStorageMatrixes is the vector they store, of a Manifold its matrix, of a horizontal lift the tuple of its blocks. So one method covers the family:\n\nNeuralNetworkParameters.freeparameters(\n    x::Union{Manifold, VectorStorageMatrix, AbstractLieAlgHorMatrix}) = parent(x)\n\nBase.parent is deliberately not used as the protocol here. parent of a SubArray is the whole underlying buffer rather than the view's own entries, so Base's relation and this one do not agree in general, and flattening on parent would silently take in far too much.","title":"Adopting it for a whole family at once"},{"category":"section","location":"protocol/#Multi-block-storage","page":"The leaf protocol","text":"freeparameters may return a Tuple or NamedTuple, for a type whose freedom lives in several places; the recursion simply continues into it, and the blocks may be structured themselves.\n\nNNP.freeparameters(g::StiefelLieAlgHorMatrix) = (A = g.A, B = g.B)\nNNP.rebuild(g::StiefelLieAlgHorMatrix, data)  = StiefelLieAlgHorMatrix(data.A, data.B, g.N, g.n)","title":"Multi-block storage"},{"category":"section","location":"protocol/#Element-type","page":"The leaf protocol","text":"flatten uses this, so a Float32 network flattens to a Vector{Float32}. Defaulting to Float64 instead would silently double the width of every single-precision network passing through, and of everything computed from the flat vector afterwards. It is also what a NetworkParameters derives its element-type parameter from, at construction.\n\nBecause every parameter set runs its constructor through this function, it is total where freeparameters is not: a leaf this package cannot read numbers out of contributes nothing to the promotion rather than raising, and so does a gap in a gradient tree. The protocol error comes from parameterlayout instead, which is where it decides something — a set that cannot be flattened is still a set with an element type.\n\nThat is also why the recursion follows freeparameters for an AbstractArray leaf only: asking an arbitrary type where its storage is would mean raising. So a leaf that keeps its numbers behind an interface that is not an array's has nothing to contribute, even with the two protocol methods defined:\n\nstruct Blocks              # not an `AbstractArray`, so nothing about it says where the numbers are\n    data::Vector{Float64}\nend\n\nNNP.freeparameters(b::Blocks) = b.data\nNNP.rebuild(::Blocks, data) = Blocks(data)\n\nparameter_eltype(NetworkParameters((L1 = (B = Blocks([1.0, 2.0]),),)))\n\n# output\n\nUnion{}\n\nflatten raises there rather than guessing an element type for numbers the promotion knows nothing about. One method more opts the leaf into the recursion, and the set flattens as itself again:\n\nNNP.parameter_eltype(b::Blocks) = parameter_eltype(freeparameters(b))\n\nps = NetworkParameters((L1 = (B = Blocks([1.0, 2.0]),),))\n(parameter_eltype(ps), first(flatten(ps)))\n\n# output\n\n(Float64, [1.0, 2.0])\n\nAn AbstractArray subtype — every structured leaf in the ecosystem — needs none of this.","title":"Element type"},{"category":"function","location":"protocol/#NeuralNetworkParameters.freeparameters","page":"The leaf protocol","text":"freeparameters(x)\n\nThe differentiable storage of a leaf x — the numbers that a flat parameter vector should contain, and the coordinates an optimizer should work in.\n\nFor an ordinary array this is the array itself. For a structured type it is whatever the type keeps its degrees of freedom in: an n times n symmetric matrix stores n(n+1)2 numbers, and it is those that belong in the flat vector — not the n^2 entries of the dense interface, which do not even have the right length.\n\nThe return value may be\n\nthe leaf itself, which marks it as terminal — the recursion stops and the numbers are copied straight out of it;\nanother array or number;\na Tuple or NamedTuple of either, for a type whose freedom lives in several blocks.\n\nTogether with rebuild this is the whole extension protocol: define the two for a type and it can be flattened, walked and written to HDF5, with nothing in this package knowing about it.\n\nExtending\n\nNeuralNetworkParameters.freeparameters(A::SymmetricMatrix) = A.S\nNeuralNetworkParameters.rebuild(A::SymmetricMatrix, data)  = SymmetricMatrix(data, A.n)\n\nThose two are the whole protocol for a leaf that is an AbstractArray subtype, which every structured parameter type in the ecosystem is. A leaf that keeps its numbers behind some other interface needs one method more, because parameter_eltype must not raise and so cannot ask an arbitrary type where its storage is:\n\nNeuralNetworkParameters.parameter_eltype(x::MyLeaf) = parameter_eltype(freeparameters(x))\n\nWithout it the leaf contributes nothing to the promoted element type, and flatten says so rather than guessing one.\n\nGeometricOptimizers already exposes exactly this relation as Base.parent for its manifolds, VectorStorageMatrixes and horizontal lifts, so one delegating method covers all of them:\n\nNeuralNetworkParameters.freeparameters(x::Union{Manifold, VectorStorageMatrix, AbstractLieAlgHorMatrix}) =\n    parent(x)\n\nBase.parent is deliberately not used as the protocol here: parent(::SubArray) is the whole underlying buffer rather than the view's own entries, so Base's relation and this one are not the same.\n\nExamples\n\nusing NeuralNetworkParameters: freeparameters\n\nA = [1.0 2.0; 3.0 4.0]\nfreeparameters(A) === A\n\n# output\n\ntrue\n\n\n\n\n\n","title":"NeuralNetworkParameters.freeparameters"},{"category":"function","location":"protocol/#NeuralNetworkParameters.rebuild","page":"The leaf protocol","text":"rebuild(prototype, data)\n\nRebuild a leaf shaped like prototype from the storage data. The inverse of freeparameters, and for an ordinary array simply data.\n\nprototype is the leaf the storage came from, which is what carries the non-differentiable information along: the n of a SymmetricMatrix, the N and n of a horizontal lift. Taking them from a prototype rather than from the type is what lets data have a different element type from prototype — ForwardDiff.Duals, when a flattened parameter set is differentiated through — and it is also why the concrete type comes back unchanged, where reconstructing from a type name can quietly return a sibling type instead.\n\nSee freeparameters for how to extend this.\n\n\n\n\n\n","title":"NeuralNetworkParameters.rebuild"},{"category":"function","location":"protocol/#NeuralNetworkParameters.parameter_eltype","page":"The leaf protocol","text":"parameter_eltype(ps)\n\nThe element type to flatten ps into: promote_type over the element types of its leaves.\n\nFloat32 parameters therefore flatten to a Vector{Float32}. Defaulting to Float64 instead — as ParameterHandling.flatten does — silently doubles the width of every single-precision network that passes through, and every quantity computed from the flat vector downstream with it.\n\nExamples\n\nusing NeuralNetworkParameters: parameter_eltype\n\nparameter_eltype((a = Float32[1, 2], b = Float32[3;;]))\n\n# output\n\nFloat32\n\nA mixed parameter set promotes:\n\nusing NeuralNetworkParameters: parameter_eltype\n\nparameter_eltype((a = Float32[1, 2], b = [3.0]))\n\n# output\n\nFloat64\n\nImplementation\n\nThis function is total, where freeparameters is not: every NetworkParameters runs its constructor through here, including sets that hold no numbers at all — a gradient tree with nothing where an untouched layer's entries would be, or SymbolicNeuralNetworks wrapping generated functions in one. A leaf this package cannot read numbers out of contributes nothing to the promotion, exactly as an empty set does, and both report Union{}. A set that cannot be flattened is still a set with an element type; the protocol error comes from parameterlayout, which decides something with it.\n\nThe recursion follows freeparameters for an AbstractArray leaf — every structured parameter type in the ecosystem — and a leaf that keeps its numbers behind another interface opts in with one method more, described under freeparameters. Asking an arbitrary type where its storage is would mean raising, which this function must not do.\n\nFor a NetworkParameters the answer is already on the type, put there by its constructor, so nothing is recomputed and the call folds to a constant.\n\n\n\n\n\n","title":"NeuralNetworkParameters.parameter_eltype"},{"category":"section","location":"library/#Library","page":"Library","text":"Everything the package exports is documented on the topical pages; this page collects the index, and the internals that the other pages refer to but do not describe in place.","title":"Library"},{"category":"section","location":"library/#Index","page":"Library","text":"","title":"Index"},{"category":"section","location":"library/#Module","page":"Library","text":"","title":"Module"},{"category":"section","location":"library/#Layout-internals","page":"Library","text":"The five concrete layouts, one per thing a parameter set is made of. They are not exported, and are constructed by parameterlayout rather than by hand, but a package storing a layout may want to dispatch on them.","title":"Layout internals"},{"category":"section","location":"library/#Classifying-a-node","page":"Library","text":"","title":"Classifying a node"},{"category":"module","location":"library/#NeuralNetworkParameters.NeuralNetworkParameters","page":"Library","text":"NeuralNetworkParameters\n\nThe parameters of a neural network, in two shapes and with conversions between them.\n\nNetworkParameters is the structured shape: a NamedTuple of NamedTuples of arrays following the architecture, wrapped in a type of its own so that the parameter set is something a package can dispatch on without piracy.\nFlatParameters is the flat shape: one AbstractVector of every number in the set, which is what a derivative, a linear solver or a quasi-Newton method wants to work with.\n\nflatten and unflatten convert between them through a ParameterLayout that is built once and reused, so a derivative taken with respect to the flat vector comes back in the shape of the network:\n\nv, layout = flatten(ps)\ng = ForwardDiff.gradient(w -> loss(unflatten(layout, w)), v)\nunflatten(layout, g)          # the gradient, laid out like `ps`\n\nStructured leaves — a symmetric matrix keeping n(n+1)2 numbers behind an n times n interface, a manifold element, a horizontal lift — plug in through two methods, freeparameters and rebuild. Everything else in the package, including the HDF5 support, is written against that protocol rather than against a list of types.\n\nLoading HDF5 brings in save and load through a package extension.\n\n\n\n\n\n","title":"NeuralNetworkParameters.NeuralNetworkParameters"},{"category":"type","location":"library/#NeuralNetworkParameters.LeafLayout","page":"Library","text":"LeafLayout(range, size, prototype)\n\nA leaf whose numbers are copied straight into the flat vector: it occupies range, and comes back reshaped to size (() for a scalar).\n\nprototype is the leaf it was built from, kept so that rebuild has something to rebuild against.\n\n\n\n\n\n","title":"NeuralNetworkParameters.LeafLayout"},{"category":"type","location":"library/#NeuralNetworkParameters.WrappedLayout","page":"Library","text":"WrappedLayout(prototype, inner)\n\nA leaf whose freeparameters are themselves structured — a SymmetricMatrix storing a vector, or a horizontal lift storing two blocks. inner is the layout of that storage; unflattening runs it and then calls rebuild on prototype.\n\n\n\n\n\n","title":"NeuralNetworkParameters.WrappedLayout"},{"category":"type","location":"library/#NeuralNetworkParameters.NestedLayout","page":"Library","text":"NestedLayout(children, range)\n\nThe layout of a NamedTuple: one child layout per key, together spanning range.\n\n\n\n\n\n","title":"NeuralNetworkParameters.NestedLayout"},{"category":"type","location":"library/#NeuralNetworkParameters.TupleLayout","page":"Library","text":"TupleLayout(children, range)\n\nThe layout of a Tuple, positional counterpart of NestedLayout.\n\n\n\n\n\n","title":"NeuralNetworkParameters.TupleLayout"},{"category":"type","location":"library/#NeuralNetworkParameters.ParametersLayout","page":"Library","text":"ParametersLayout(inner)\n\nThe layout of a NetworkParameters: the layout of the NamedTuple it wraps, tagged so that unflattening returns a NetworkParameters again rather than a bare NamedTuple.\n\n\n\n\n\n","title":"NeuralNetworkParameters.ParametersLayout"},{"category":"function","location":"library/#NeuralNetworkParameters.isparametertree","page":"Library","text":"isparametertree(x)\n\nWhether x is a branch of a parameter set — a NetworkParameters, NamedTuple or Tuple that the walks recurse into — as opposed to a leaf.\n\n\n\n\n\n","title":"NeuralNetworkParameters.isparametertree"},{"category":"function","location":"library/#NeuralNetworkParameters.isterminal","page":"Library","text":"isterminal(x)\n\nWhether the numbers of the leaf x can be copied straight out of it, i.e. whether freeparameters returns x itself.\n\n\n\n\n\n","title":"NeuralNetworkParameters.isterminal"},{"category":"section","location":"representations/#The-two-representations","page":"The two representations","text":"","title":"The two representations"},{"category":"section","location":"representations/#Structured","page":"The two representations","text":"NetworkParameters{T} wraps a NamedTuple and forwards getproperty, getindex, keys, values, length, iterate and pairs to it, so it reads like the NamedTuple it holds. NamedTuple(ps) unwraps it again, and is the same thing as params; the conversion is defined here rather than downstream because a package that owns neither Base.NamedTuple nor the type cannot define it without committing piracy on both counts.\n\nThe wrapper is not decoration. A bare NamedTuple belongs to Base, so any package wanting to give a parameter set its own behaviour — saving it, flattening it, stepping an optimizer over it — must write methods whose every argument type is somebody else's. That is type piracy, and two packages doing it can silently disagree about the same call. Owning the type removes the problem at the root.\n\nThe T is the element type the leaves promote to, carried on the type so that a method signature can bind it — f(ps::NetworkParameters{T}) where {T}, or a Union with AbstractVector{T}. A set is built from its keys and values with NetworkParameters(NamedTuple{keys}(vals)), since the braces name the element type rather than the keys, and it is derived rather than chosen: writing NetworkParameters{T}(nt) asserts T and raises if the leaves say otherwise. See parameter_eltype for what the promotion does and does not guarantee.\n\nNote that key order is part of the identity of a parameter set:\n\nusing NeuralNetworkParameters\n\nNetworkParameters((a = [1.0], b = [2.0])) == NetworkParameters((b = [2.0], a = [1.0]))\n\n# output\n\nfalse\n\nwhich is why the HDF5 writer records it explicitly — see Reading and writing HDF5.","title":"Structured"},{"category":"section","location":"representations/#Flat","page":"The two representations","text":"FlatParameters is an AbstractVector, so it works with anything that takes one; parent gives the bare Vector for code that would rather not see a wrapper.\n\nsimilar deliberately keeps the layout, so scratch space derived from a flat parameter set — a gradient, a momentum buffer — stays self-describing:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nfp = FlatParameters(ps)\ng = similar(fp)\nflatlayout(g) == flatlayout(fp)\n\n# output\n\ntrue\n\nIndividual layers can be read back off the flat form, which is mostly useful interactively:\n\nusing NeuralNetworkParameters\n\nfp = FlatParameters(NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),)))\nfp.L1.b\n\n# output\n\n1-element Vector{Float64}:\n 3.0","title":"Flat"},{"category":"type","location":"representations/#NeuralNetworkParameters.NetworkParameters","page":"The two representations","text":"NetworkParameters(params::NamedTuple)\nNetworkParameters{T}(params::NamedTuple)\nNetworkParameters{T, Keys, ValueTypes}(params::NamedTuple)\n\nThe parameters of a neural network: a NamedTuple whose entries follow the architecture, wrapped in a type of its own.\n\nThe wrapper exists so that the parameters of a network are a type somebody owns. A bare NamedTuple belongs to Base, so a package that wants to give the parameter set its own behaviour — saving it to file, flattening it, stepping an optimizer over it — has to write methods on a signature in which it owns nothing. Every such method is type piracy, and two packages doing it can silently disagree.\n\nEntries are reached with getproperty or getindex; the underlying NamedTuple is params.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]), L2 = (W = [4.0;;],)))\nps.L1.W\n\n# output\n\n1×2 Matrix{Float64}:\n 1.0  2.0\n\nBoth forms of access agree, and keys reports the layers:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]), L2 = (W = [4.0;;],)))\n(ps[:L2] === ps.L2, keys(ps))\n\n# output\n\n(true, (:L1, :L2))\n\nElement type\n\nThe first type parameter is the element type the leaves promote to, so that T binds in a method signature:\n\nf(ps::NetworkParameters{T}) where {T} = ...\ng(x::Union{AbstractVector{T}, NetworkParameters{T}}) where {T} = ...\n\nThat is the reason it is a type parameter and not only a function. GeometricOptimizers takes the element type from the type of the solution it is handed, and a parameter set could not join its OptimizerSolution{T} union while it carried no such parameter.\n\nIt is derived by parameter_eltype at construction and never chosen; naming it, as NetworkParameters{T}(params), asserts it and raises if the leaves say otherwise. Note that it is a promotion, not a guarantee of uniformity — a mixed set reports the type its leaves promote to while each leaf keeps its own:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = Float32[1 2], b = [3.0]),))\n(ps isa NetworkParameters{Float64}, eltype(ps.L1.W))\n\n# output\n\n(true, Float32)\n\nA set with nothing to promote — an empty one, or a gradient tree that is all gaps — reports Union{}.\n\nImplementation\n\ngetproperty is overloaded to reach into the wrapped NamedTuple, so the field itself has to be read with getfield — which is what params does.\n\nThere is deliberately no Base.eltype: this type forwards the NamedTuple interface, for which eltype means the type of what iteration yields — a layer's NamedTuple — rather than the numeric element type. The numeric one is parameter_eltype.\n\n\n\n\n\n","title":"NeuralNetworkParameters.NetworkParameters"},{"category":"function","location":"representations/#NeuralNetworkParameters.params","page":"The two representations","text":"params(p::NetworkParameters)\n\nThe NamedTuple wrapped by p.\n\nAlso the accessor a NeuralNetwork uses for its own parameters in AbstractNeuralNetworks, hence the short name.\n\n\n\n\n\n","title":"NeuralNetworkParameters.params"},{"category":"type","location":"representations/#NeuralNetworkParameters.FlatParameters","page":"The two representations","text":"FlatParameters(data, layout)\nFlatParameters(ps)\n\nA parameter set in its flat form: an AbstractVector of every number in the set, carrying the ParameterLayout that puts it back together.\n\nThis is the representation to differentiate with respect to. It behaves as an ordinary vector, so ForwardDiff, a linear solver or an optimizer can work on it directly, while still knowing how to return an answer in the shape of the network. parent hands out the bare Vector for anything that would rather not see a wrapper.\n\nsimilar keeps the layout, so scratch space derived from a flat parameter set — a gradient, a momentum buffer — stays self-describing.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nfp = FlatParameters(ps)\n(length(fp), fp[2])\n\n# output\n\n(3, 2.0)\n\nEntries can be read back by layer, and the whole set converted:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nfp = FlatParameters(ps)\n(fp.L1.b, NetworkParameters(fp) == ps)\n\n# output\n\n([3.0], true)\n\n\n\n\n\n","title":"NeuralNetworkParameters.FlatParameters"},{"category":"function","location":"representations/#NeuralNetworkParameters.flatlayout","page":"The two representations","text":"flatlayout(fp)\n\nThe ParameterLayout carried by fp.\n\n\n\n\n\n","title":"NeuralNetworkParameters.flatlayout"},{"category":"section","location":"hdf5/#Reading-and-writing-HDF5","page":"Reading and writing HDF5","text":"Loading HDF5 brings in the storage methods through a package extension, so the binary chain behind HDF5 is only paid for by users who ask for it. The four entry points are not exported — save and load are names that would collide with almost anything — so they are reached as NeuralNetworkParameters.save, or imported explicitly.","title":"Reading and writing HDF5"},{"category":"section","location":"hdf5/#Round-trip","page":"Reading and writing HDF5","text":"using NeuralNetworkParameters, HDF5\nusing NeuralNetworkParameters: save, load\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nsave(\"parameters.h5\", ps)\nload(NetworkParameters, \"parameters.h5\") == ps      # true\n\nThe element type is preserved: a Float32 network reloads as Float32.","title":"Round trip"},{"category":"section","location":"hdf5/#Key-order-is-recorded","page":"Reading and writing HDF5","text":"HDF5 hands back the members of a group sorted, so a network with ten layers would read back as L1, L10, L2, … — and since key order is part of the identity of a parameter set, that set no longer compares equal to the one that was written. Guessing the order back from the names, by sorting on a trailing integer, only works for names that happen to look like L10.\n\nThe writer therefore records the order explicitly, as an attribute on each group, and the reader uses it. Files that predate this — see below — still fall back to the guess, because for them there is nothing better available.","title":"Key order is recorded"},{"category":"section","location":"hdf5/#Structured-parameters","page":"Reading and writing HDF5","text":"A structured leaf is written as a group holding its freeparameters and its parameter_metadata — the latter only when there is any, so a type that keeps everything in its storage, as a manifold element does, writes the storage alone. Reading it back needs to know how to reconstruct the type, and there are two ways to supply that.\n\nAgainst a prototype. Pass a parameter set of the right shape and the leaves are rebuilt with rebuild, exactly as unflattening does. Nothing needs registering. Where the architecture is to hand — and when loading a network's parameters it usually is — this is the simpler path:\n\nload(NetworkParameters, \"parameters.h5\", prototype)\n\nFrom the registry. For a file that must be readable on its own, the package owning the type registers a reconstructor:\n\nNeuralNetworkParameters.parameter_metadata(A::SymmetricMatrix) = (n = A.n,)\n\nregister_parameter_type!(\"SymmetricMatrix\", (S, md) -> SymmetricMatrix(S, md.n))\n\nLoading a file with an unregistered type raises an error naming the type and both remedies, rather than failing obscurely.","title":"Structured parameters"},{"category":"section","location":"hdf5/#Older-files","page":"Reading and writing HDF5","text":"Parameter files written before this package still load:\n\nthose written by AbstractNeuralNetworks, which are plain nested groups with no attributes;\nthose written by GeometricMachineLearning, whose structured matrices carry a gml_type attribute. Such a group holds the type's fields under their own names and no storage for rebuild to take, so the registry is the only way in: the type has to have been registered, and a prototype is no substitute. Passing one says so rather than failing obscurely.\n\nThe group's fields reach the registered reconstructor in both argument positions, storage and metadata alike, since a file in that layout records nothing that could tell them apart. A reconstructor that means to read these files sorts that out itself — and reaches for its fields by name, the layout having recorded no key order either.","title":"Older files"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.save","page":"Reading and writing HDF5","text":"save(h5, ps)\nsave(filename, ps)\n\nWrite a parameter set to an open HDF5 store or to a file. Implemented in the HDF5Ext extension.\n\n\n\n\n\n","title":"NeuralNetworkParameters.save"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.load","page":"Reading and writing HDF5","text":"load(NetworkParameters, h5)\nload(NetworkParameters, h5, prototype)\n\nRead a parameter set back. Implemented in the HDF5Ext extension.\n\nThe three-argument form rebuilds the leaves against prototype, a parameter set of the right shape, and is the form that needs no registration — rebuild has something to work from. The two-argument form has to reconstruct structured leaves from what the file says about them, which is what register_parameter_type! is for.\n\n\n\n\n\n","title":"NeuralNetworkParameters.load"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.h5save","page":"Reading and writing HDF5","text":"h5save(h5, x, path)\n\nWrite x into the HDF5 store h5 at path. Implemented in the HDF5Ext extension; load HDF5 to get the methods.\n\n\n\n\n\n","title":"NeuralNetworkParameters.h5save"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.h5load","page":"Reading and writing HDF5","text":"h5load(h5)\n\nRead back what h5save wrote. Implemented in the HDF5Ext extension.\n\n\n\n\n\n","title":"NeuralNetworkParameters.h5load"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.register_parameter_type!","page":"Reading and writing HDF5","text":"register_parameter_type!(name, reconstruct)\n\nTeach load how to rebuild a structured leaf that was stored under name, when there is no prototype to rebuild against.\n\nreconstruct(storage, metadata) receives what freeparameters produced and the parameter_metadata that went with it, and returns the leaf:\n\nNeuralNetworkParameters.parameter_metadata(A::SymmetricMatrix) = (n = A.n,)\nregister_parameter_type!(\"SymmetricMatrix\", (S, md) -> SymmetricMatrix(S, md.n))\n\nA package that owns a parameter type registers it in its own __init__. Nothing here needs to know the type exists, which is the point: with the structured matrix types living upstream of the package that trains with them, the only way to serialise them without somebody committing type piracy is for the serialiser to be driven by a protocol rather than by a list.\n\nThe registry is only consulted by the two-argument load; the prototype form bypasses it.\n\n\n\n\n\n","title":"NeuralNetworkParameters.register_parameter_type!"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.parameter_metadata","page":"Reading and writing HDF5","text":"parameter_metadata(x)\n\nThe non-differentiable fields of a leaf, as a NamedTuple, for the benefit of storage formats that have no prototype to rebuild against.\n\nEmpty by default. rebuild recovers this information from its prototype, which is enough for flattening — but a parameter set read back from a file has no prototype, so a type whose storage does not determine it (the n of a SymmetricMatrix does follow from length(S); the N and n of a StiefelLieAlgHorMatrix do not) has to write it out. See register_parameter_type!.\n\n\n\n\n\n","title":"NeuralNetworkParameters.parameter_metadata"},{"category":"function","location":"hdf5/#NeuralNetworkParameters.parameter_type_name","page":"Reading and writing HDF5","text":"parameter_type_name(x)\n\nThe name a structured leaf is stored under, nameof(typeof(x)) by default. Paired with register_parameter_type!.\n\n\n\n\n\n","title":"NeuralNetworkParameters.parameter_type_name"},{"category":"section","location":"conversions/#Converting-between-them","page":"Converting between them","text":"","title":"Converting between them"},{"category":"section","location":"conversions/#The-layout","page":"Converting between them","text":"A layout is an ordinary value: build it once, keep it, compare it. That is the difference from returning a closure that undoes the flattening — a closure cannot be stored in an optimizer cache, compared for equality, or inferred through.","title":"The layout"},{"category":"section","location":"conversions/#The-conversions","page":"Converting between them","text":"","title":"The conversions"},{"category":"section","location":"conversions/#Derivatives","page":"Converting between them","text":"The flat form exists to be differentiated. Forward mode needs nothing special, because unflatten is generic in the element type of its vector:\n\nusing NeuralNetworkParameters, ForwardDiff\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nv, layout = flatten(ps)\n\nf(w) = sum(abs2, unflatten(layout, w).L1.W)\nForwardDiff.gradient(f, v)\n\n# output\n\n3-element Vector{Float64}:\n 2.0\n 4.0\n 0.0\n\nand the result is turned back into the shape of the network by unflattening it again:\n\nunflatten(layout, ForwardDiff.gradient(f, v)).L1.W\n\n# output\n\n1×2 Matrix{Float64}:\n 2.0  4.0\n\nReverse mode works through the same two functions: there are ChainRulesCore rules for both, and at a fixed layout they are linear and mutually adjoint, so each rule is the other conversion. A position the reverse pass says nothing about — a layer the loss never touched — comes back as a zero block rather than an error.\n\nFor a Jacobian, unflatten also accepts a matrix and splits its rows by parameter block:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\n_, layout = flatten(ps)\nJ = reshape(collect(1.0:9.0), 3, 3)\nunflatten(layout, J).L1.b\n\n# output\n\n1×3 Matrix{Float64}:\n 3.0  6.0  9.0","title":"Derivatives"},{"category":"section","location":"conversions/#Cost,-and-why-it-is-a-copy","page":"Converting between them","text":"Flattening copies. On a network of 1.3 million Float64 parameters the round trip costs about 0.3 ms, against roughly 0.95 ms for a single forward pass at batch size 32 — so a full conversion is some 10 % of one forward pass, and less of a training step.\n\nSharing storage instead, so that the flat vector and the leaves were views of each other, would not work even setting cost aside: unflatten has to be able to produce parameters of a different element type from the ones the layout was built from, which is exactly what forward-mode differentiation needs. A Float64 buffer cannot be shared with a Dual-valued view.\n\nWhere repeated conversion does matter is an inner loop — an optimizer flattening twice per inner product. That is what flatten! and unflatten! are for: given a layout and a buffer, both allocate nothing when called from compiled code. (From an uninferred context, such as the top level of a script, the recursion is not specialised and costs a few tens of bytes per leaf on Julia 1.10 — which is what the package's own allocation tests measure inside a function to avoid.)\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nv, layout = flatten(ps)\nbuffer = similar(v)\nflatten!(buffer, ps, layout)        # warm up, then this allocates nothing\nbuffer == v\n\n# output\n\ntrue","title":"Cost, and why it is a copy"},{"category":"type","location":"conversions/#NeuralNetworkParameters.ParameterLayout","page":"Converting between them","text":"ParameterLayout\n\nWhere each leaf of a parameter set lands in its flat vector, and what has to be done to put it back.\n\nA layout is an ordinary value, built once by parameterlayout and then reusable: it can be stored in an optimizer cache, handed to a solver, or compared with ==. The alternative — returning a closure that undoes the flattening, as ParameterHandling.flatten does — is none of those things, and a chain of closures is not type stable either.\n\nThe five concrete layouts mirror the five things a parameter set is made of: ParametersLayout for a NetworkParameters, NestedLayout for a NamedTuple, TupleLayout for a Tuple, WrappedLayout for a leaf with structured storage, and LeafLayout for one whose numbers are copied directly.\n\n\n\n\n\n","title":"NeuralNetworkParameters.ParameterLayout"},{"category":"function","location":"conversions/#NeuralNetworkParameters.parameterlayout","page":"Converting between them","text":"parameterlayout(ps)\n\nBuild the ParameterLayout of ps in one walk.\n\nThe leaves are laid out in the order they are encountered, depth first, so the flat vector reads like the parameter set does.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]), L2 = (W = [4.0;;],)))\nlayout = parameterlayout(ps)\n(length(layout), parameterrange(layout.inner.children.L1.children.b))\n\n# output\n\n(4, 3:3)\n\n\n\n\n\n","title":"NeuralNetworkParameters.parameterlayout"},{"category":"function","location":"conversions/#NeuralNetworkParameters.parameterrange","page":"Converting between them","text":"parameterrange(layout)\n\nThe stretch of the flat vector that layout occupies.\n\n\n\n\n\n","title":"NeuralNetworkParameters.parameterrange"},{"category":"function","location":"conversions/#NeuralNetworkParameters.flatlength","page":"Converting between them","text":"flatlength(ps)\n\nThe number of entries ps flattens to, without allocating the flat vector.\n\nDeliberately not called parameterlength: AbstractNeuralNetworks has a function of that name for the parameter count of a model, and the two would collide on using.\n\n\n\n\n\n","title":"NeuralNetworkParameters.flatlength"},{"category":"function","location":"conversions/#NeuralNetworkParameters.flatten","page":"Converting between them","text":"flatten(ps)\nflatten(T, ps)\n\nCopy every number of ps into one flat Vector, and return it together with the ParameterLayout needed to put it back.\n\nWithout T the element type is parameter_eltype(ps), i.e. the parameters' own — a Float32 network flattens to a Vector{Float32}.\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nv, layout = flatten(ps)\nv\n\n# output\n\n3-element Vector{Float64}:\n 1.0\n 2.0\n 3.0\n\nunflatten is its inverse:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nv, layout = flatten(ps)\nunflatten(layout, v) == ps\n\n# output\n\ntrue\n\nImplementation\n\nThe copy is a copyto! per leaf over a known range, so it runs at memory bandwidth and works unchanged for GPU arrays — no element is ever indexed individually.\n\nCopying rather than viewing is deliberate. A flat vector of views could not carry a different element type from the parameters, and that is exactly what differentiating through the flat form needs: the unflatten on the forward pass of ForwardDiff has to produce Dual-valued parameters over a Dual-valued vector. See flatten! for the allocation-free form used in inner loops.\n\n\n\n\n\n","title":"NeuralNetworkParameters.flatten"},{"category":"function","location":"conversions/#NeuralNetworkParameters.flatten!","page":"Converting between them","text":"flatten!(v, ps, [layout])\n\nWrite the numbers of ps into the existing vector v, and return v.\n\nAllocation-free when the layout is supplied and the call is made from compiled code, which is the point: an optimizer that flattens its parameters once per step, or twice per inner product, should not allocate a fresh vector each time. (Called from an uninferred context — the top level of a script, say — the recursion is not specialised and costs a few tens of bytes per leaf on Julia 1.10.)\n\nv, layout = flatten(ps)          # once\nflatten!(v, ps, layout)          # per iteration, zero allocations\n\n\n\n\n\n","title":"NeuralNetworkParameters.flatten!"},{"category":"function","location":"conversions/#NeuralNetworkParameters.unflatten","page":"Converting between them","text":"unflatten(layout, v)\n\nRebuild the parameter set that layout describes from the flat vector v.\n\nv may have a different element type from the parameters the layout was built from — that is what makes the flat form usable for derivatives:\n\ng = ForwardDiff.gradient(w -> loss(unflatten(layout, w)), v)\nunflatten(layout, g)     # the gradient, in the shape of the parameters\n\nExamples\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0], b = [3.0]),))\nv, layout = flatten(ps)\nunflatten(layout, [10.0, 20.0, 30.0]).L1.W\n\n# output\n\n1×2 Matrix{Float64}:\n 10.0  20.0\n\nImplementation\n\nEach leaf is copied out of v rather than viewed into it, so the leaves are ordinary arrays and cannot alias each other or the flat vector.\n\n\n\n\n\nunflatten(layout, J::AbstractMatrix)\n\nSplit the rows of J into the shape of the parameter set — for a Jacobian taken with respect to the flat vector, so that the block belonging to each parameter can be read off.\n\nEach leaf becomes the n_mathrmleaf times mathrmsize(J 2) row block it occupies. The leaves are not rebuilt: a block of a Jacobian is not a parameter, so there is nothing to rebuild it into.\n\n\n\n\n\nunflatten(fp::FlatParameters)\n\nThe structured form of fp, using the layout it carries.\n\n\n\n\n\n","title":"NeuralNetworkParameters.unflatten"},{"category":"function","location":"conversions/#NeuralNetworkParameters.unflatten!","page":"Converting between them","text":"unflatten!(ps, layout, v)\n\nWrite the numbers of v into the leaves of the existing ps, and return ps.\n\nAllocation-free on the same terms as flatten!, whose counterpart it is. The write goes through freeparameters, so only the storage of a structured leaf is touched — a SymmetricMatrix has its n(n+1)/2 numbers replaced and stays symmetric.\n\nRequires mutable leaves; a parameter set with a scalar leaf has to use the out-of-place unflatten.\n\n\n\n\n\n","title":"NeuralNetworkParameters.unflatten!"},{"category":"section","location":"#NeuralNetworkParameters","page":"Home","text":"The parameters of a neural network, in two shapes, with conversions between them.\n\nThe package serves the networks defined in AbstractNeuralNetworks, GeometricMachineLearning and SymbolicNeuralNetworks, and holds nothing but the parameters: it depends on none of them, and adds one dependency of its own (ChainRulesCore).\n\nIt exists for two reasons. The first is that a parameter set needs to be a type somebody owns. A bare NamedTuple belongs to Base, so a package that wants to give the parameter set its own behaviour — writing it to a file, flattening it, stepping an optimizer over it — has to write methods on a signature in which it owns nothing. Every such method is type piracy, and two packages doing it can silently disagree.\n\nThe second is that the traversal which goes with the container was being written once per package. Recurse into the NamedTuples, do something at each leaf, put the result back in the same shape: that is flatten/unflatten, h5save/h5load, changebackend, map_to_cpu and the elementwise optimizer primitives, each re-declaring a method per structured parameter type. Written once against the leaf protocol below, one implementation covers all of them and needs to know none of the types.\n\nA note on the name: the package is NeuralNetworkParameters, the type it exports is NetworkParameters. A package cannot export a type sharing its own name — the module binding wins at the using site, so NeuralNetworkParameters(nt) would try to call a Module. AbstractNeuralNetworks 0.7 dropped the name rather than aliasing it, so that one type has one name across the ecosystem: code written against NeuralNetworkParameters as a type reaches for NetworkParameters here instead.","title":"NeuralNetworkParameters"},{"category":"section","location":"#The-two-shapes","page":"Home","text":"NetworkParameters follows the architecture — a NamedTuple of NamedTuples of arrays, one entry per layer:\n\nusing NeuralNetworkParameters\n\nps = NetworkParameters((L1 = (W = [1.0 2.0; 3.0 4.0], b = [5.0, 6.0]),\n                        L2 = (W = [7.0 8.0], b = [9.0])))\nps.L1.W\n\n# output\n\n2×2 Matrix{Float64}:\n 1.0  2.0\n 3.0  4.0\n\nFlatParameters is the same numbers as one vector, which is the shape a derivative, a linear solver or a quasi-Newton method wants:\n\nfp = FlatParameters(ps)\ncollect(fp)\n\n# output\n\n9-element Vector{Float64}:\n 1.0\n 3.0\n 2.0\n 4.0\n 5.0\n 6.0\n 7.0\n 8.0\n 9.0\n\nNeither is derived from the other on the fly: flatten and unflatten convert between them through a ParameterLayout that is built once and then reused.","title":"The two shapes"},{"category":"section","location":"#Differentiating-with-respect-to-the-flat-form","page":"Home","text":"The point of the flat shape is that ForwardDiff — or any method that wants a vector — can work on it, while the answer still comes back laid out like the network:\n\nusing ForwardDiff\n\nv, layout = flatten(ps)\nloss(p) = sum(model(x, p))\n\ng = ForwardDiff.gradient(w -> loss(unflatten(layout, w)), v)\nunflatten(layout, g)          # the gradient, one entry per layer\n\nunflatten is generic in the element type of its vector, which is what makes this work: a Dual-valued vector produces Dual-valued parameters. Reverse mode is covered too — there are ChainRulesCore rules for both conversions, so Zygote can differentiate through them, and with ZygoteRules loaded the gradient of a NetworkParameters is a NetworkParameters rather than a tangent nobody can consume.","title":"Differentiating with respect to the flat form"},{"category":"section","location":"#Structured-parameters","page":"Home","text":"A parameter need not be a plain array. A symmetric matrix keeps n(n+1)2 numbers behind an n times n interface; a manifold element or a horizontal lift keeps its own. Those numbers — not the dense entries — are what belongs in the flat vector, and what an optimizer should move.\n\nTwo methods teach the package about such a type, and everything else follows: flattening, the tree walks, and HDF5. See The leaf protocol.","title":"Structured parameters"},{"category":"section","location":"#Pages","page":"Home","text":"Pages = [\"representations.md\", \"conversions.md\", \"protocol.md\", \"walks.md\", \"hdf5.md\", \"library.md\"]\nDepth = 1","title":"Pages"}]
}
